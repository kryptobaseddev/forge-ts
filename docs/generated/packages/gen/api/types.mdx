---
title: gen — Types
description: Type contracts for the gen package
---

Type contracts exported by this package: interfaces, type aliases, and enums.

## GuideSource

The source heuristic that discovered a guide.

```typescript
GuideSource
```

## DiscoveredGuide

A guide discovered from the symbol graph by code analysis heuristics.

| Property      | Type            | Required | Description                                             |
| ------------- | --------------- | -------- | ------------------------------------------------------- |
| `slug`        | `string`        | Yes      | URL-safe slug (e.g. "configuration", "error-handling"). |
| `title`       | `string`        | Yes      | Human-readable title (e.g. "Configuration Guide").      |
| `description` | `string`        | Yes      | Short description of the guide's content.               |
| `source`      | `GuideSource`   | Yes      | Which heuristic discovered this guide.                  |
| `symbols`     | `ForgeSymbol[]` | Yes      | The symbols that contribute content to this guide.      |

## MdText

Inline leaf node: literal text.

| Property | Type     | Required | Description                          |
| -------- | -------- | -------- | ------------------------------------ |
| `type`   | `"text"` | Yes      | Node discriminant — always `"text"`. |
| `value`  | `string` | Yes      | The raw text content.                |

## MdInlineCode

Inline leaf node: code span.

| Property | Type           | Required | Description                                            |
| -------- | -------------- | -------- | ------------------------------------------------------ |
| `type`   | `"inlineCode"` | Yes      | Node discriminant — always `"inlineCode"`.             |
| `value`  | `string`       | Yes      | The code span content (without surrounding backticks). |

## MdStrong

Inline container: strong emphasis (bold).

| Property   | Type           | Required | Description                            |
| ---------- | -------------- | -------- | -------------------------------------- |
| `type`     | `"strong"`     | Yes      | Node discriminant — always `"strong"`. |
| `children` | `MdPhrasing[]` | Yes      | Inline content rendered in bold.       |

## MdEmphasis

Inline container: emphasis (italic).

| Property   | Type           | Required | Description                              |
| ---------- | -------------- | -------- | ---------------------------------------- |
| `type`     | `"emphasis"`   | Yes      | Node discriminant — always `"emphasis"`. |
| `children` | `MdPhrasing[]` | Yes      | Inline content rendered in italic.       |

## MdLink

Inline container: hyperlink.

| Property   | Type           | Required | Description                               |
| ---------- | -------------- | -------- | ----------------------------------------- |
| `type`     | `"link"`       | Yes      | Node discriminant — always `"link"`.      |
| `url`      | `string`       | Yes      | The link destination URL.                 |
| `children` | `MdPhrasing[]` | Yes      | Inline content rendered as the link text. |

## MdPhrasing

Union of all inline (phrasing) content types.

```typescript
MdPhrasing
```

## MdHeading

Block node: heading (depth 1-6).

| Property   | Type                         | Required | Description                                                 |
| ---------- | ---------------------------- | -------- | ----------------------------------------------------------- |
| `type`     | `"heading"`                  | Yes      | Node discriminant — always `"heading"`.                     |
| `depth`    | `1 \| 2 \| 3 \| 4 \| 5 \| 6` | Yes      | Heading level: 1 for `#`, 2 for `##`, up to 6 for `######`. |
| `children` | `MdPhrasing[]`               | Yes      | Inline content of the heading.                              |

## MdParagraph

Block node: paragraph.

| Property   | Type           | Required | Description                               |
| ---------- | -------------- | -------- | ----------------------------------------- |
| `type`     | `"paragraph"`  | Yes      | Node discriminant — always `"paragraph"`. |
| `children` | `MdPhrasing[]` | Yes      | Inline content of the paragraph.          |

## MdCode

Block node: fenced code block.

| Property | Type                          | Required | Description                                      |
| -------- | ----------------------------- | -------- | ------------------------------------------------ |
| `type`   | `"code"`                      | Yes      | Node discriminant — always `"code"`.             |
| `lang`   | `string \| null \| undefined` | No       | Language tag for syntax highlighting.            |
| `value`  | `string`                      | Yes      | The code content (without the fence delimiters). |

## MdBlockquote

Block node: blockquote.

| Property   | Type           | Required | Description                                |
| ---------- | -------------- | -------- | ------------------------------------------ |
| `type`     | `"blockquote"` | Yes      | Node discriminant — always `"blockquote"`. |
| `children` | `MdBlock[]`    | Yes      | Block-level content inside the quote.      |

## MdHtml

Block node: raw HTML (including comments).

| Property | Type     | Required | Description                          |
| -------- | -------- | -------- | ------------------------------------ |
| `type`   | `"html"` | Yes      | Node discriminant — always `"html"`. |
| `value`  | `string` | Yes      | The raw HTML string.                 |

## MdThematicBreak

Block node: horizontal rule.

| Property | Type              | Required | Description                                   |
| -------- | ----------------- | -------- | --------------------------------------------- |
| `type`   | `"thematicBreak"` | Yes      | Node discriminant — always `"thematicBreak"`. |

## MdListItem

List item container.

| Property   | Type                   | Required | Description                                        |
| ---------- | ---------------------- | -------- | -------------------------------------------------- |
| `type`     | `"listItem"`           | Yes      | Node discriminant — always `"listItem"`.           |
| `spread`   | `boolean \| undefined` | No       | Whether blank lines separate this item's children. |
| `children` | `MdBlock[]`            | Yes      | Block-level content of the list item.              |

## MdList

Block node: ordered or unordered list.

| Property   | Type                   | Required | Description                                          |
| ---------- | ---------------------- | -------- | ---------------------------------------------------- |
| `type`     | `"list"`               | Yes      | Node discriminant — always `"list"`.                 |
| `ordered`  | `boolean \| undefined` | No       | `true` for numbered lists, `false` for bullet lists. |
| `spread`   | `boolean \| undefined` | No       | Whether blank lines separate list items.             |
| `children` | `MdListItem[]`         | Yes      | The list items.                                      |

## MdTableCell

GFM table cell.

| Property   | Type           | Required | Description                               |
| ---------- | -------------- | -------- | ----------------------------------------- |
| `type`     | `"tableCell"`  | Yes      | Node discriminant — always `"tableCell"`. |
| `children` | `MdPhrasing[]` | Yes      | Inline content of the cell.               |

## MdTableRow

GFM table row.

| Property   | Type            | Required | Description                              |
| ---------- | --------------- | -------- | ---------------------------------------- |
| `type`     | `"tableRow"`    | Yes      | Node discriminant — always `"tableRow"`. |
| `children` | `MdTableCell[]` | Yes      | Cells in this row.                       |

## MdTable

GFM table.

| Property   | Type                                                     | Required | Description                                  |
| ---------- | -------------------------------------------------------- | -------- | -------------------------------------------- |
| `type`     | `"table"`                                                | Yes      | Node discriminant — always `"table"`.        |
| `align`    | `("left" \| "center" \| "right" \| null)[] \| undefined` | No       | Column alignment directives for each column. |
| `children` | `MdTableRow[]`                                           | Yes      | Rows of the table (first row is the header). |

## MdBlock

Union of all block content types.

```typescript
MdBlock
```

## MdRoot

Document root.

| Property   | Type        | Required | Description                              |
| ---------- | ----------- | -------- | ---------------------------------------- |
| `type`     | `"root"`    | Yes      | Node discriminant — always `"root"`.     |
| `children` | `MdBlock[]` | Yes      | Top-level block content of the document. |

## FrontmatterResult

Result of parsing frontmatter from markdown/MDX content.

| Property | Type                      | Required | Description                                     |
| -------- | ------------------------- | -------- | ----------------------------------------------- |
| `body`   | `string`                  | Yes      | The body content without the frontmatter block. |
| `data`   | `Record<string, unknown>` | Yes      | The parsed frontmatter data as a key-value map. |

## DocPage

A single generated documentation page.

| Property      | Type                                          | Required | Description                                                                                                                                                                                                                                                                        |
| ------------- | --------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `path`        | `string`                                      | Yes      | Relative path from outDir (e.g., "packages/core/index.md")                                                                                                                                                                                                                         |
| `content`     | `string`                                      | Yes      | Page content (Markdown or MDX)                                                                                                                                                                                                                                                     |
| `frontmatter` | `Record<string, string \| number \| boolean>` | Yes      | Frontmatter fields                                                                                                                                                                                                                                                                 |
| `stub`        | `boolean \| undefined`                        | No       | When true, this page is scaffolding intended for human/agent editing. Stub pages are created only on the first build and never overwritten, preserving manual edits across subsequent `forge-ts build` runs. Auto-generated pages (stub=false) are always regenerated from source. |

## SiteGeneratorOptions

Options controlling the doc site generator.

| Property             | Type                                                                 | Required | Description                                       |
| -------------------- | -------------------------------------------------------------------- | -------- | ------------------------------------------------- |
| `format`             | `"markdown" \| "mdx"`                                                | Yes      | Output format                                     |
| `ssgTarget`          | `"docusaurus" \| "mintlify" \| "nextra" \| "vitepress" \| undefined` | No       | SSG target for frontmatter.                       |
| `projectName`        | `string`                                                             | Yes      | Project name                                      |
| `projectDescription` | `string \| undefined`                                                | No       | Project description.                              |
| `repositoryUrl`      | `string \| undefined`                                                | No       | Repository URL (auto-detected from package.json). |
| `packageName`        | `string \| undefined`                                                | No       | npm package name for install commands.            |

## SSGTarget

Supported SSG target identifiers.

```typescript
SSGTarget
```

## GeneratedFile

A file to write to disk during scaffolding or generation.

| Property  | Type                   | Required | Description                                                                                                                                                                                                                                                                      |
| --------- | ---------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `path`    | `string`               | Yes      | Relative path from the docs output directory.                                                                                                                                                                                                                                    |
| `content` | `string`               | Yes      | File content (string for text).                                                                                                                                                                                                                                                  |
| `stub`    | `boolean \| undefined` | No       | When true, this file is scaffolding intended for human/agent editing. Stub files are created only on the first build and never overwritten, preserving manual edits across subsequent `forge-ts build` runs. Callers should check this flag and skip writing if the file exists. |

## SSGStyleGuide

Style guide configuration for the SSG target.

| Property              | Type                            | Required | Description                                                    |
| --------------------- | ------------------------------- | -------- | -------------------------------------------------------------- |
| `pageExtension`       | `"mdx" \| "md"`                 | Yes      | File extension for doc pages.                                  |
| `supportsMdx`         | `boolean`                       | Yes      | Whether the target supports MDX components.                    |
| `requiresFrontmatter` | `boolean`                       | Yes      | Whether frontmatter is required on every page.                 |
| `maxHeadingDepth`     | `number`                        | Yes      | Maximum recommended heading depth.                             |
| `defaultImports`      | `string[]`                      | Yes      | Component imports to add at top of MDX files (if supportsMdx). |
| `codeBlockLanguage`   | `"typescript" \| "ts" \| "tsx"` | Yes      | Code block language for TypeScript examples.                   |

## ScaffoldManifest

Scaffold manifest describing what `init docs` creates.

| Property          | Type                     | Required | Description                              |
| ----------------- | ------------------------ | -------- | ---------------------------------------- |
| `target`          | `SSGTarget`              | Yes      | The SSG target this manifest is for.     |
| `files`           | `GeneratedFile[]`        | Yes      | Files that will be created.              |
| `dependencies`    | `Record<string, string>` | Yes      | npm dependencies to install.             |
| `devDependencies` | `Record<string, string>` | Yes      | npm devDependencies to install.          |
| `scripts`         | `Record<string, string>` | Yes      | Scripts to add to package.json.          |
| `instructions`    | `string[]`               | Yes      | Post-scaffold instructions for the user. |

## AdapterContext

Context passed to adapter methods.

| Property             | Type                  | Required | Description                                    |
| -------------------- | --------------------- | -------- | ---------------------------------------------- |
| `config`             | `ForgeConfig`         | Yes      | Resolved forge-ts configuration.               |
| `projectName`        | `string`              | Yes      | Project name (from package.json or directory). |
| `projectDescription` | `string \| undefined` | No       | Project description.                           |
| `pages`              | `DocPage[]`           | Yes      | The generated doc pages (from site-generator). |
| `symbols`            | `ForgeSymbol[]`       | Yes      | All symbols extracted from the project.        |
| `outDir`             | `string`              | Yes      | Output directory for generated docs.           |

## DevServerCommand

Command to start a local dev server for doc preview.

| Property | Type       | Required | Description                                  |
| -------- | ---------- | -------- | -------------------------------------------- |
| `bin`    | `string`   | Yes      | The binary to execute (e.g., "npx", "node"). |
| `args`   | `string[]` | Yes      | Arguments to pass to the binary.             |
| `cwd`    | `string`   | Yes      | Working directory to run from.               |
| `label`  | `string`   | Yes      | Human-readable label for the command.        |
| `url`    | `string`   | Yes      | The URL the dev server will be available at. |

## SSGAdapter

The central SSG adapter interface. Every doc platform provider implements this contract. One file per provider. No shared mutable state.

| Property         | Type                                                             | Required | Description                                                                                                                                             |
| ---------------- | ---------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `target`         | `SSGTarget`                                                      | Yes      | Unique target identifier.                                                                                                                               |
| `displayName`    | `string`                                                         | Yes      | Human-readable display name.                                                                                                                            |
| `styleGuide`     | `SSGStyleGuide`                                                  | Yes      | Style guide for this platform.                                                                                                                          |
| `scaffold`       | `(context: AdapterContext) => ScaffoldManifest`                  | Yes      | Generate the complete scaffold for a new doc site. Called by `forge-ts init docs --target <name>`. Returns all files, dependencies, and scripts needed. |
| `transformPages` | `(pages: DocPage[], context: AdapterContext) => GeneratedFile[]` | Yes      | Transform generic DocPages into platform-specific pages. Adds correct frontmatter, component imports, file extensions. Called during `forge-ts build`.  |
| `generateConfig` | `(context: AdapterContext) => GeneratedFile[]`                   | Yes      | Generate platform-specific configuration files. e.g., mint.json, sidebars.js, \_meta.json, .vitepress/config.ts Called during `forge-ts build`.         |
| `getDevCommand`  | `(outDir: string) => DevServerCommand`                           | Yes      | Get the command to start the local dev server for this platform. Called by `forge-ts docs dev`.                                                         |
| `detectExisting` | `(outDir: string) => Promise<boolean>`                           | Yes      | Check if a scaffold already exists in the output directory. Used for safety checks before init or target change.                                        |

## MarkdownOptions

Options controlling Markdown output.

| Property | Type                   | Required | Description                                    |
| -------- | ---------------------- | -------- | ---------------------------------------------- |
| `mdx`    | `boolean \| undefined` | No       | Whether to use MDX syntax (default: Markdown). |

## ReadmeSyncOptions

Options controlling README sync behaviour.

| Property          | Type                   | Required | Description                                                     |
| ----------------- | ---------------------- | -------- | --------------------------------------------------------------- |
| `badge`           | `boolean \| undefined` | No       | Include a "Documented with forge-ts" badge above the API table. |
| `includeExamples` | `boolean \| undefined` | No       | Include first                                                   |

## SkillPackage

A generated skill package following the agentskills.io directory structure. Contains SKILL.md plus optional references and scripts files.

| Property        | Type                                   | Required | Description                                                       |
| --------------- | -------------------------------------- | -------- | ----------------------------------------------------------------- |
| `directoryName` | `string`                               | Yes      | The skill directory name (lowercase, hyphens only, max 64 chars). |
| `files`         | `{ path: string; content: string; }[]` | Yes      | Files to write inside the skill directory.                        |

## SSGConfigFile

A single generated SSG configuration file.

| Property  | Type     | Required | Description                                                  |
| --------- | -------- | -------- | ------------------------------------------------------------ |
| `path`    | `string` | Yes      | Relative path from outDir (e.g., "mint.json", "\_meta.json") |
| `content` | `string` | Yes      | File content                                                 |

## GenerateOptions

Options for the generation pipeline.

| Property     | Type                   | Required | Description                                                                                                                                                                                                                                                |
| ------------ | ---------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `forceStubs` | `boolean \| undefined` | No       | When true, overwrite stub pages even if they already exist on disk. Normally stub pages (concepts, guides, faq, contributing, changelog) are only created on the first build to preserve manual edits. Use this to reset stubs to their scaffolding state. |
