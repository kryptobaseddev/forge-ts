---
title: enforcer — Types
outline: deep
description: Type contracts for the enforcer package
---

# enforcer — Types

Type contracts exported by this package: interfaces, type aliases, and enums.

## FormatOptions

Options that control how  renders its output.

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `colors` | `boolean` | Yes | Emit ANSI colour escape sequences when `true`. |
| `verbose` | `boolean` | Yes | When `true`, include the symbol's type signature alongside each diagnostic so the reader has immediate context. |
