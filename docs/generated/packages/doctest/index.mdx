---
title: doctest
description: doctest package overview
---

npm package: `@forge-ts/doctest`

## Installation

```bash
npm install @forge-ts/doctest
```

## Description

The `doctest` package.

## Key Concepts

The pipeline has three stages: **extract** (`extractExamples`) reads the `ForgeSymbol[]` graph and collects every fenced code block found inside an `@example` tag; **generate** (`generateTestFiles`) wraps each snippet in a `node:test` `it()` call and writes the result to a cache directory; **run** (`runTests`) spawns the Node test runner against those files and parses TAP output into structured `TestCaseResult` records.  The top-level `doctest()` function composes all three stages. Individual stages are also exported so callers can customise caching, filtering, or test output handling without re-running the full pipeline.  Key exports: - `doctest` — Run the full extract → generate → run pipeline. - `extractExamples` — Collect `@example` code blocks from a symbol graph. - `generateTestFiles` — Wrap extracted examples in `node:test` scaffolding. - `runTests` — Execute generated test files and return structured results. - `ExtractedExample` — A single extracted code snippet with source location. - `VirtualTestFile` — A generated test file path and content pair. - `RunResult` / `TestCaseResult` — Structured test execution outcome types. - `GeneratorOptions` — Options for `generateTestFiles` (e.g., `cacheDir`).

## Quick Usage

```typescript
import { loadConfig } from "@forge-ts/core";
import { doctest } from "@forge-ts/doctest";

const config = await loadConfig();
const result = await doctest(config);
if (!result.success) {
  console.error(`${result.errors.length} doctest failure(s)`);
  process.exit(1);
}
```

## Functions & Classes

| Symbol                                                                           | Kind     | Description                                                                      |
| -------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------- |
| [`extractExamples()`](/packages/doctest/reference/functions#extractexamples)     | function | Extracts all `@example` blocks from a list of `ForgeSymbol` objects.             |
| [`generateTestFiles()`](/packages/doctest/reference/functions#generatetestfiles) | function | Generates a virtual test file for a set of extracted examples.  Each example...  |
| [`runTests()`](/packages/doctest/reference/functions#runtests)                   | function | Writes virtual test files to disk and executes them with Node 24 native TypeS... |
| [`doctest()`](/packages/doctest/reference/functions#doctest)                     | function | Runs the full doctest pipeline: extract → generate → run.                        |
| [`extractExamples()`](/packages/doctest/reference/functions#extractexamples)     | function | Extracts all `@example` blocks from a list of `ForgeSymbol` objects.             |
| [`generateTestFiles()`](/packages/doctest/reference/functions#generatetestfiles) | function | Generates a virtual test file for a set of extracted examples.  Each example...  |
| [`runTests()`](/packages/doctest/reference/functions#runtests)                   | function | Writes virtual test files to disk and executes them with Node 24 native TypeS... |
| [`doctest()`](/packages/doctest/reference/functions#doctest)                     | function | Runs the full doctest pipeline: extract → generate → run.                        |

## Types & Interfaces

| Symbol                                                                   | Kind      | Description                                                    |
| ------------------------------------------------------------------------ | --------- | -------------------------------------------------------------- |
| [`ExtractedExample`](/packages/doctest/reference/types#extractedexample) | interface | A single extracted `@example` block ready for test generation. |
| [`GeneratorOptions`](/packages/doctest/reference/types#generatoroptions) | interface | Options for virtual test file generation.                      |
| [`VirtualTestFile`](/packages/doctest/reference/types#virtualtestfile)   | interface | A generated virtual test file.                                 |
| [`RunResult`](/packages/doctest/reference/types#runresult)               | interface | Result of running the generated test files.                    |
| [`TestCaseResult`](/packages/doctest/reference/types#testcaseresult)     | interface | The result of a single test case.                              |
| [`ExtractedExample`](/packages/doctest/reference/types#extractedexample) | interface | A single extracted `@example` block ready for test generation. |
| [`GeneratorOptions`](/packages/doctest/reference/types#generatoroptions) | interface | Options for virtual test file generation.                      |
| [`VirtualTestFile`](/packages/doctest/reference/types#virtualtestfile)   | interface | A generated virtual test file.                                 |
| [`RunResult`](/packages/doctest/reference/types#runresult)               | interface | Result of running the generated test files.                    |
| [`TestCaseResult`](/packages/doctest/reference/types#testcaseresult)     | interface | The result of a single test case.                              |

## Other Exports

| Symbol                                                                     | Kind | Description                                                                      |
| -------------------------------------------------------------------------- | ---- | -------------------------------------------------------------------------------- |
| [`extractor.ts`](/packages/doctest/reference/functions#extractorts)        | file |                                                                                  |
| [`generator.ts`](/packages/doctest/reference/functions#generatorts)        | file |                                                                                  |
| [`runner.ts`](/packages/doctest/reference/functions#runnerts)              | file |                                                                                  |
| [`index.ts`](/packages/doctest/reference/functions#indexts)                | file | TSDoc `@example` block extractor and test runner for forge-ts projects.  Extr... |
| [`tsup.config.ts`](/packages/doctest/reference/functions#tsupconfigts)     | file |                                                                                  |
| [`vitest.config.ts`](/packages/doctest/reference/functions#vitestconfigts) | file |                                                                                  |
| [`doctest.test.ts`](/packages/doctest/reference/functions#doctesttestts)   | file |                                                                                  |
| [`extractor.ts`](/packages/doctest/reference/functions#extractorts)        | file |                                                                                  |
| [`generator.ts`](/packages/doctest/reference/functions#generatorts)        | file |                                                                                  |
| [`runner.ts`](/packages/doctest/reference/functions#runnerts)              | file |                                                                                  |
| [`index.ts`](/packages/doctest/reference/functions#indexts)                | file | forge-ts/doctest — TSDoc `@example` block extractor and test runner.  Extract... |
| [`doctest.test.ts`](/packages/doctest/reference/functions#doctesttestts)   | file |                                                                                  |
